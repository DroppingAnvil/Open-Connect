package dev.droppinganvil.v3.utils.permissions;

/**
 * The permissions package is meany to be embedded and built upon so while some actions may exist as constants there is a high chance it will not
 */
public enum Actions {
    ADD_ENTRY,
    EDIT_ENTRY,
    READ,
    WRITE,
    MODIFY,
}
